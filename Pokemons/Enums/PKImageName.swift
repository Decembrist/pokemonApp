import Foundation

enum PKImageName {
    
    enum Pokemon: String {
        case mainIcon = "flame.fill"
        case buttonType = "person"
    }
    
    enum Search: String {
        case mainIcon = "magnifyingglass"
    }

    enum Settings: String {
        case mainIcon = "gear"
    }
}
